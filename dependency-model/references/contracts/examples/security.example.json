{
  "contract_version": "1.0.0",
  "target": "/abs/path/to/repo",
  "seeded_by": { "contract": "profile:topology", "contract_version": "1.0.0" },
  "scan": { "scan_version": "1.0.0", "confidence": "high" },
  "categories": {
    "security": {
      "status": "discovered",
      "dependencies": [
        {
          "id": "security:postgres-db-password",
          "name": "POSTGRES_PASSWORD",
          "evidence": ["deploy/k8s/secrets.yaml:8", "internal/db/pool.go:41"],
          "related_ids": ["service:postgres-primary", "config:database-url"],
          "resilience": {
            "timeout": null,
            "retry": null,
            "fallback": null,
            "health_check": null,
            "on_path": ["startup"]
          },
          "details": {
            "kind": "secret",
            "provider": "kubernetes",
            "scope": null,
            "granted_to": ["internal/db/pool.go"],
            "rotation_declared": null
          }
        }
      ],
      "assumptions": [
        {
          "claim": "the secret is scoped to the database component only",
          "why_unconfirmed": "the kubernetes secret manifest does not restrict which pods may mount it"
        }
      ]
    }
  }
}
