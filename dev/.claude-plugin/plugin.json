{
  "name": "dev",
  "version": "2.0.0",
  "description": "Developer toolkit (sem-powered): annotate code with durable SEM@<sha> intent markers (sem-annotate), and find dead code + duplication and produce a ranked plan (dedupe). Uses the sem CLI entity graph. Supports Go, TypeScript/JavaScript, and Python.",
  "author": { "name": "efitz" }
}
